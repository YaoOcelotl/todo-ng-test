import { DeleteCompletedResult } from './delete-completed-result';

describe('DeleteCompletedResult', () => {
  it('should create an instance', () => {
    expect(new DeleteCompletedResult()).toBeTruthy();
  });
});
